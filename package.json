{
  "name": "bubble-shooter-game",
  "version": "1.0.0",
  "description": "A fun bubble shooter game",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev": "vite",
    "compile": "hardhat compile",
    "deploy:irys": "hardhat run scripts/deploy.js --network irysTestnet"
  },
  "dependencies": {
    "@irys/sdk": "^0.2.11",
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.26.0"
  }
}
